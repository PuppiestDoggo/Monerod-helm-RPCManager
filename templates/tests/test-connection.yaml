apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monerod-RPCManager.fullname" . }}-test-connection"
  labels:
    {{- include "monerod-RPCManager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monerod-RPCManager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
