apiVersion: v1
kind: Service
metadata:
  name: {{ include "monerod-RPCManager.fullname" . }}
  labels:
    {{- include "monerod-RPCManager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monerod-RPCManager.selectorLabels" . | nindent 4 }}
